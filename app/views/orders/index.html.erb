<h1>Orders</h1>

<div id="orders-list">
  <% if @orders.present? %>
    <% @orders.each do |order| %>
      <%= render partial: "order", locals: { order: order } %>
    <% end %>
  <% else %>
    <%= render partial: "no_orders" %>
  <% end %>
</div>
